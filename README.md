# <!DOCTYPE html><html lang="ko"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>The New Adventure of Peter Pan</title><style>*{    margin:0;    padding:0;    box-sizing:border-box;    font-family:Arial,sans-serif;}body{    overflow:hidden;    background:#02142b;}canvas{    display:block;    background:linear-gradient(#63c7ff,#001b44);}#ui{    position:absolute;    top:10px;    left:10px;    color:white;    z-index:10;    text-shadow:2px 2px 5px black;}#story{    margin-top:10px;    max-width:320px;    line-height:1.5;}.mobile{    position:absolute;    bottom:20px;    right:20px;    display:flex;    flex-direction:column;    gap:10px;}.btn{    width:80px;    height:80px;    border:none;    border-radius:50%;    font-size:35px;    background:white;    opacity:0.8;}.btn:active{    transform:scale(0.9);}</style></head><body><div id="ui">    <h2>✨ Peter Pan Adventure</h2>    <div>⭐ Score: <span id="score">0</span></div>    <div>❤️ HP: <span id="hp">5</span></div>    <div id="story">        🌙 Peter Pan arrives in London.<br>        🏠 Wendy, John and Michael join him.<br>        ✨ They fly to Neverland.<br>        🏕️ Meet Lost Boys & Tiger Lily.<br>        ⚔️ Fight Captain Hook and pirates!    </div></div><canvas id="game"></canvas><div class="mobile">    <button class="btn" id="up">⬆️</button>    <button class="btn" id="down">⬇️</button></div><script>const canvas = document.getElementById("game");const ctx = canvas.getContext("2d");canvas.width = window.innerWidth;canvas.height = window.innerHeight;let score = 0;let hp = 5;const scoreEl = document.getElementById("score");const hpEl = document.getElementById("hp");const player = {    x:80,    y:300,    w:50,    h:80,    speed:7};const aiPirate = {    x:window.innerWidth - 100,    y:300,    w:40,    h:120,    speed:3};const enemies = [];const friends = [];const stars = [];for(let i=0;i<120;i++){    stars.push({        x:Math.random()*canvas.width,        y:Math.random()*canvas.height,        r:Math.random()*2    });}function spawnEnemy(){    enemies.push({        x:canvas.width + 50,        y:Math.random()*(canvas.height-100),        w:50,        h:50,        speed:4 + Math.random()*2    });}function spawnFriend(){    const names=["Wendy","John","Michael","Tiger Lily","Lost Boy"];    friends.push({        x:canvas.width + 50,        y:Math.random()*(canvas.height-100),        w:60,        h:60,        speed:3,        name:names[Math.floor(Math.random()*names.length)]    });}setInterval(spawnEnemy,1800);setInterval(spawnFriend,4000);let up=false;let down=false;document.addEventListener("keydown",(e)=>{    if(e.key==="ArrowUp") up=true;    if(e.key==="ArrowDown") down=true;});document.addEventListener("keyup",(e)=>{    if(e.key==="ArrowUp") up=false;    if(e.key==="ArrowDown") down=false;});document.getElementById("up").ontouchstart=()=>up=true;document.getElementById("up").ontouchend=()=>up=false;document.getElementById("down").ontouchstart=()=>down=true;document.getElementById("down").ontouchend=()=>down=false;function drawSky(){    ctx.fillStyle="#001b44";    ctx.fillRect(0,0,canvas.width,canvas.height);    for(let s of stars){        ctx.fillStyle="white";        ctx.beginPath();        ctx.arc(s.x,s.y,s.r,0,Math.PI*2);        ctx.fill();        s.x -= 1;        if(s.x<0){            s.x = canvas.width;            s.y = Math.random()*canvas.height;        }    }}function drawPeterPan(){    ctx.fillStyle="#00ff88";    // body    ctx.fillRect(player.x,player.y,player.w,player.h);    // head    ctx.beginPath();    ctx.arc(player.x+25,player.y-15,20,0,Math.PI*2);    ctx.fill();    // sword    ctx.fillStyle="silver";    ctx.fillRect(player.x+50,player.y+20,40,5);    // hat    ctx.fillStyle="green";    ctx.beginPath();    ctx.moveTo(player.x+10,player.y-25);    ctx.lineTo(player.x+25,player.y-50);    ctx.lineTo(player.x+40,player.y-25);    ctx.fill();    // wings magic    ctx.fillStyle="rgba(255,255,0,0.7)";    ctx.beginPath();    ctx.arc(player.x-10,player.y+20,10,0,Math.PI*2);    ctx.fill();}function drawPirate(){    ctx.fillStyle="red";    ctx.fillRect(aiPirate.x,aiPirate.y,aiPirate.w,aiPirate.h);    ctx.fillStyle="black";    ctx.fillRect(aiPirate.x-10,aiPirate.y+20,20,5);    // head    ctx.beginPath();    ctx.arc(aiPirate.x+20,aiPirate.y-10,20,0,Math.PI*2);    ctx.fill();}function drawEnemies(){    enemies.forEach((e,index)=>{        ctx.fillStyle="purple";        ctx.fillRect(e.x,e.y,e.w,e.h);        ctx.fillStyle="white";        ctx.fillText("☠️",e.x+5,e.y+35);        e.x -= e.speed;        if(collide(player,e)){            enemies.splice(index,1);            hp--;            hpEl.textContent = hp;        }        if(e.x < -100){            enemies.splice(index,1);        }    });}function drawFriends(){    friends.forEach((f,index)=>{        ctx.fillStyle="#ffe066";        ctx.fillRect(f.x,f.y,f.w,f.h);        ctx.fillStyle="black";        ctx.fillText(f.name,f.x-10,f.y-10);        f.x -= f.speed;        if(collide(player,f)){            friends.splice(index,1);            score += 10;            scoreEl.textContent = score;        }        if(f.x<-100){            friends.splice(index,1);        }    });}function collide(a,b){    return(        a.x < b.x+b.w &&        a.x+a.w > b.x &&        a.y < b.y+b.h &&        a.y+a.h > b.y    );}function update(){    if(up) player.y -= player.speed;    if(down) player.y += player.speed;    if(player.y<0) player.y=0;    if(player.y+player.h>canvas.height)        player.y = canvas.height-player.h;    // pirate AI follows player    if(aiPirate.y < player.y)        aiPirate.y += aiPirate.speed;    if(aiPirate.y > player.y)        aiPirate.y -= aiPirate.speed;}function gameOver(){    ctx.fillStyle="rgba(0,0,0,0.7)";    ctx.fillRect(0,0,canvas.width,canvas.height);    ctx.fillStyle="white";    ctx.font="60px Arial";    ctx.fillText("GAME OVER",canvas.width/2-180,canvas.height/2);    ctx.font="30px Arial";    ctx.fillText(        "Captain Hook Wins...",        canvas.width/2-150,        canvas.height/2+60    );}function animate(){    ctx.clearRect(0,0,canvas.width,canvas.height);    drawSky();    update();    drawPeterPan();    drawPirate();    drawEnemies();    drawFriends();    if(hp<=0){        gameOver();        return;    }    requestAnimationFrame(animate);}animate();</script></body></html>peter-pan-adventure-game
HTML, CSS, JavaScript를 사용하여 the new adventure of peterpan 애니메이션 모바일 시뮬레이터 액션 어드벤처 게임을 만드세요. 플레이어는 위/아래 방향키로 왼쪽 패들을 조작하고, 오른쪽 패들은 공격모드로 컴퓨터가 조작합니다. 게임에는 피터팬이 주인공으로 런던에 가서 웬디, 존, 마이클을 데리고 네버랜드에 도착합니다 거기서 잃어버린 아이들을 만나 재밌게 놀고 뛰고 인디언들 타이거릴리 등을 만나 또한 해적들과 대결하는것등이 포함되어야 합니다.
